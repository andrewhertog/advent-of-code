package main

type file struct {
	name string
	path []string
	size int
}

func main() {
	// parse through list of commands creating file objects for each, then create a function
	// that sorts by path, then start recursing through each to find the appropate dir with
	// size > that 10000
}
